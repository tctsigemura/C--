//
// t380.cmm : 複雑な論理式(2012.09.08 追加)
//

int     a,b,c,d,e;
boolean s,t,u,v,w;

void f() {
  if (((a>b)==(b>c)) && c>d || a>b && b>c) {
   a=1;
  } else {
   a=2;
  }
  v = ((a>b)==(b>c)) && c>d || a>b && b>c;

  if ((s==t) && a+b>=0 || u && v) {
   a=3;
  } else {
   a=4;
  }
//  w = (s==t) && a+b>=0 || u && v;
  a = (a+b+c+d+e+a*b+b*c+c*d*e)*ord(((s==t) && a+b>=0 || u && v) &&
    (s==t) && a+b>=0 || u && v);
}
